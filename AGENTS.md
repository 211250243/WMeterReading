# 项目开发约定

## 用户偏好

这是一个以识别效果为中心的小项目。使用中文交流。结构简洁、命名直接、代码可读，便于后续不同开发者修改和扩展。用户已确认先复用公共数据和现有模型，打通自动定位、字轮识别、指针识别、完整读数计算，再针对错误补业务数据。

## 目录与代码

- 外部源码只放 `references/`；我们开发的代码只放 `meter_reader/`；数据、权重、产物分别放 `data/`、`weights/`、`outputs/`。
- 同一功能只有一条主实现路径。先用普通函数或简单类，接口清楚即可；不要提前建立注册器、插件系统、抽象基类体系。
- 模块只按真实职责拆分，不因猜测未来需求创建大量目录。不要把整个上游应用的页面、服务和训练系统复制到主项目里。
- 路径在一个配置文件中集中设置，以新工作目录为基准；不要依赖从哪个文件夹运行。旧目录绝对路径仅用于首次导入素材。
- 源码、数据、模型不混放。新增模型时只加必要模块/配置，不改变其它模块的输入输出习惯。
- 外部代码和模型来源在 `docs/REFERENCES.md` 简短记录，保留其必要许可证说明，不扩展成独立管理系统。

## 保持轻量

不建立审计、哈希/指纹、冒烟测试、测试平台、CI/CD、模型注册中心、数据版本平台、微服务或复杂日志基础设施。不从旧规划恢复这些内容。

保留对功能直接有用的检查：图片能否读取、区域是否有效、读数是否缺位、模型是否可加载。用少量真实图片、结果可视化、简单读数例子验证功能；不搭建形式化验收流程或大量测试代码。必要训练信息和错误直接打印，结果保存为易读文件。

## 正确性与工作方式

- 不能用人工框或真值伪装自动识别；不能用假输出伪装模型已经运行。
- 分开保存可见字符、模型结果和人工读数。无法可靠读全时保留部分结果和简短原因。
- 真正影响读数的倍率、方向、翻字和进位处理不能因简化工程而省略。
- 优先完成可用功能；一般实现选择自主决定，不反复让用户确认。遇到缺少算力/权重等条件时继续独立可完成的部分，并如实说明未完成内容。
- `docs/PLAN.md` 是简化后的实施依据，覆盖旧工作目录早期计划中的复杂工程要求。
