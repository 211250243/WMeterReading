# 本次实际运行

## 2026-09-23迁移前修正

本轮全部真实产物在`outputs/pre5090/`；原`weights/`推理权重和旧人工记录没有覆盖。只运行小批训练以检查代码，无GPU或充分训练结果。

- detector：4张训练/4张验证，640、全参数，实际前后向、best/last保存与恢复至第2轮；后处理与正常推理共用，新增IoU 0.8指标。
- geometry：4图29区域，从原图做小角度旋转和框位置/尺度扰动，实际训练与恢复；`augmentation.jpg`展示实际网络输入及对应角点。
- wheel：5训练窗/3验证窗，全参数；另验证数字+v共12类字典的头迁移、训练恢复和checkpoint推理。第二轮同分时best保持第一轮。这个样本量不能证明准确率提升。
- `crop_comparison/`：12张含字轮公共原图。12字轮全部匹配到自动框，11成功校正；直接裁剪/校正后的可见字符匹配分别4/5。42指针匹配到39框、38成功校正；单盘原始类别匹配分别20/21；仅5个校正区域达到四角IoU 0.8，14个方向误差超过15度。标注仅事后匹配、计分和第三列对照，前两列识别不使用标注区域或方向。这不是整表准确率。
- `field_predictions/`：4张业务原图处理无运行错误，但0张确认完整值；`automatic/`：16张公共原图完整正确0、全部未确定。不能把运行成功当作精度改善。
- `previous_complete/`：旧2张已给出完整值的样例复跑仍为`06948.952`、`05549.079`。
- Pose数据导出在`data/prepared/pose/`：3975/991图、28448/7062区域；训练622、验证158个图外角点屏蔽坐标监督，其余角点保留语义顺序。所有导出行已核对格式/范围及图片对应；联合模型未训练。
- 页面已在浏览器打开：批量读数表、独立字轮/指针显示、相对路径预览正常，人工记录折叠。新结果项目内路径可随目录迁移，旧绝对路径记录不重写。

当前仍需可靠的全表倍率/布局、字轮翻字消歧和充分训练。执行状态见PLAN第7节，命令见README。

## 以下为旧基线历史记录

本机 Windows、Python 3.11、torch 2.6.0+cpu，无可用 CUDA，约15 GB内存。旧目录未修改，新项目不依赖旧目录。

已准备3975张训练图、991张验证图，保留作者划分并跳过指定34张；字轮920/235个。预览在 `data/prepared/previews/`。

作者指针 demo 实际输出 `08024667`。初始12张公共验证图的真值裁剪诊断为指针序列11/12正确、字轮可见标签1/2正确；使用真值区域，不能代表自动结果。见 `outputs/crop_diagnostic/summary.json`。

最初有限 CPU 自动基线的12张公共验证原图：完整正确0、全部待核对。检测、方向、倍率的错误都会影响整表；不能靠拒绝困难图声称高准确率。记录在 `outputs/auto_baseline/`。

## 最终固定权重的实际结果

结果位置：

- `outputs/evaluation_automatic/`：96张公共验证原图，`summary.json`、`comparison.json`、JSON/CSV与逐图裁剪。
- `outputs/predictions/`：53张业务照片，全部原图自动处理完成，0个运行错误，53张待核对。没有正式业务真值，因此不计算业务准确率。
- `outputs/evaluation_crops/`：32张真值裁剪诊断，指针序列28/32正确；其中3个字轮窗口的可见标签1/3正确。仅用于定位识别模块的问题。

**自动整表：96张中9张完整正确，87张待核对，整体完整正确率9.4%。** 21张的组合候选与标签一致（包含已给出完整值的9张），其余候选仍有疑问，不能当确认读数。当前9个完整结果均为8盘全指针表，不代表带字轮混合表已经可靠跑通。文件名中数字没有参与识别，例如文件名含 `06948.953` 的照片实际模型输出 `06948.952`，并与公共标签一致。

这不是生产准确率，也不是独立业务测试。不能用已接收的9张全部正确来宣称100%准确率。主要问题仍是图像倍率/表型预测不够可靠、指针上下文低分、漏盘、字轮裁剪及翻字消歧。

## 本机实际训练情况

| 模块 | 本轮训练 | 模块验证（不等于自动整表） |
|---|---|---|
| RT-DETRv2-R18 | 320分辨率，冻结主干；256图4轮，再全3975图1轮；最终epoch5 | 96张验证图，IoU≥0.5且分数>0.45的匹配召回92.2%，精度95.7%，非mAP |
| 有向四角/倍率/表型 | 512图10轮，再全量7轮，最后全量字轮加权采样4轮；最终epoch21 | 192张验证图中的1345个真值区域，四角归一化距离0.0556；字轮区域0.0437；倍率分类99.93%，表型99.41% |
| OpenOCR SVTRv2 字轮 | 全920窗口，冻结编码器微调8轮；最终epoch8 | 全235验证窗口可见标签125/235完全一致，53.2%，包含 v 标记 |
| WMeter 指针 | 作者权重，保留旧 CPU 修复 | 新环境 demo 实际输出08024667 |

四角模块指标使用真值区域，其高分不能代表在自动检测框或业务图上的效果。当前没有 GPU 训练结果。CPU 的检测全量一轮约23分钟，640分辨率及充分微调更适合在服务器继续。

旧基线权重在 `weights/detector/meter.pt`、`weights/geometry.pt`、`weights/wheel/meter.pt`。保存了模型和优化器；新实验用README的`--init`命令载入，恢复同一实验用`--resume`。历史训练过程在 `outputs/training_*.txt`；早期阶段主动结束后保留最后完整轮，未完成轮不冒充完成。

## 已核对的功能和剩余工作

- 新环境安装、CPU模型加载、批量原图识别、逐图保存、JSON/CSV、可编辑安装后从其它目录运行均已实际执行。
- 检测320→640权重迁移及全主干反向传播实际运行；CUDA本机不可用，未宣称已跑GPU。
- 原图改成不带读数的文件名后，检测、四角、位权、识别输出保持一致。
- 页面已打开实际结果，保存空人工读数和明确的界面验证备注、再识别本图，人工记录保持独立且未被覆盖。
- Decimal重复位权、冲突、缺位、9→0和提前翻字候选已用直观例子核对。
- 没有 `v` 的窗口也可能提前显示下一整数；例如00481与低位.8333保留00481.8333 / 00480.8333两种候选，不强行减一。`v` 的可靠语义与完整字轮翻字消歧仍需完成。
- 仍需提高自动检测框上的方向/倍率稳定性，继续字轮微调，并针对53张业务图的实际失败补正式标注。现有业务人工记录与模型预测都没有作为训练真值。
