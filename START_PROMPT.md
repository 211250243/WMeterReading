请在当前工作目录开始搭建并实现一个完整的水表识别项目。先阅读根目录 `AGENTS.md`、`docs/PLAN.md`、`docs/CONTEXT.md`、`docs/REFERENCES.md`，然后直接开始工作，不要再做一轮大范围调研，也不要停在目录骨架或计划上。

我的目标：输入原始水表照片，自动定位字轮窗口及全部读数小表盘，自动裁剪/校正，分别识别字轮和指针，结合倍率与进位关系计算完整读数，最终能够批量处理并人工核对结果。部署在服务器或电脑上，准确率优先。先用公开 WMeter5K 和现有权重把主要流程跑通，后续需要时我再单独标注业务数据。

开发风格非常重要：这是一个小项目。结构必须简洁、可读、易改，方便不同的人继续优化和加功能。不要引入审计、哈希/指纹系统、冒烟测试、测试平台、模型注册中心、复杂日志系统、插件框架、微服务或其它与当前功能无关的工程建设。不为了“规范”创建很多空文件或抽象层。必要验证用少量真实图片、可视化和实际运行完成，进位算法用直观例子核对即可。请将时间花在识别功能和准确率上。

目录约定：
- `references/`：外部参考项目源码，所有实际引入的上游仓库都放这里；暂时不用的只保留链接，不必全部下载。
- `meter_reader/`：我们的主项目，所有新开发的识别、计算、训练、可视化代码放这里。
- `data/`：原始图片和转换后的训练数据。
- `weights/`：模型权重。
- `outputs/`：训练结果、识别结果和预览。
- `docs/`：简短必要的说明。

旧工作目录是 `B:\Development\Projects\WMeter_Reader`。请按 CONTEXT.md 复制必要素材到新工作目录，保留旧目录及其人工核对记录。不要直接复制旧虚拟环境、缓存、历史审计文件和整套杂乱目录。已有 WMeter-Reader 本地代码修复过 CPU 推理，优先复用这一份，而不是重新下载后丢掉修复。

首版候选组合：RT-DETRv2 区域检测、区域四角/方向校正、OpenOCR/SVTRv2 字轮识别、现有 WMeter 指针模型、我们自己实现的位权与进位计算。它们是起点，不是不可更改的要求；如果某组件实际难以接入，可在已有候选中选更直接的可用方案，并说明理由。只接入当前要用的模型，后续通过统一的小接口替换比较，不把多个完整项目混在一起。

请依次推进：整理素材和环境 → 准备公共数据 → 接通识别模块 → 训练/接通自动定位与方向校正 → 实现整表计算 → 批量入口和简单人工复核页面。训练和集成可以根据依赖调整先后，过程中持续用真实图片查看结果。模型训练需要先检查实际硬件；原环境只是 CPU 版，不能据此认定机器没有 GPU。若当前条件不足，完成不依赖该条件的工作，再明确说明需要的资源，不要把未训练的模块说成已完成。

请特别注意：
1. 正常推理只输入图片，不使用人工 ROI、文件名中的数字或标签中的正确答案。
2. 字轮、指针、完整读数必须分别展示，不能再把只有指针的结果当成整表结果。
3. 小指针的倍率和顺序不能随意猜；翻字、临界进位和重复位权要处理。确实不确定时清楚标出，别凑出一个完整数字。
4. 不把个人核对记录或模型预测直接当训练真值。
5. 字轮区域标签可以训练整行识别，第一版不要求我先补逐位数字框、针尖或像素掩膜。
6. 先运行一套完整方案，后续再逐个比较更好的组件；不要一次性把所有候选都实现。

每完成一段说明现在实际跑通了什么、结果在哪里、还有什么问题。README 写明真实可用的安装、训练、识别和查看结果命令。最终交付应是可以实际继续训练和使用的项目，不是只有接口、模拟结果或占位函数的展示。
