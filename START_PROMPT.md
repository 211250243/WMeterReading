# 迁移后继续开发提示

继续当前水表识别项目。先完整阅读根目录 `AGENTS.md`、`docs/PLAN.md`、`docs/CONTEXT.md`、`docs/RESULTS.md`、`docs/REFERENCES.md` 和 `README.md`，以文档记录的真实运行结果为准，不重新开展一轮大范围调研。

项目已迁移到带 NVIDIA RTX 5090 的 Windows 电脑。先检查实际 GPU、驱动、CUDA 版 PyTorch、数据、权重和训练入口是否可用；不要复制或沿用旧电脑的 `.venv`。正常推理只输入原始图片，不使用人工 ROI、文件名数字或标签答案。

真实应用场景是很多块水表，各自正上方有固定正向摄像头，需要批量自动读取。当前先不考虑局部强光。目标是准确展示字轮结果、各小表盘结果和经过倍率、位权、翻字及进位处理后的完整读数；信息不足时明确保留不确定状态，不能猜出完整数字。人工记录只用于必要对照，不是产品目标，也不能直接作为训练真值。

迁移前已完成可运行基线、三个训练入口及其关键修正、联合四角训练数据导出、批量入口和结果页面；迁移前 CPU 小批训练只验证代码可运行，不能视为有效精度权重。详细完成项、缺陷和真实指标见 `docs/PLAN.md` 第 6、7 节及 `docs/RESULTS.md`。

接下来从 `docs/PLAN.md` 第 7 节未完成项继续：

1. 先接入并验证“区域类别 + 有序四角”的联合定位方案，减少检测框与后续 RegionNet 两阶段误差。
2. 在 RTX 5090 上充分训练、验证，并用自动原图推理可视化检查裁剪范围、角点顺序和方向。
3. 使用可靠裁剪评估字轮 SVTRv2 和现有 WMeter 指针模型；只有证据表明识别器自身仍是主要误差源时，才微调或更换识别器。
4. 修正倍率、表盘顺序、重复位权、翻字和临界进位，无法确认的表型保持未解析。
5. 用公开 WMeter5K 和少量真实图片持续检查端到端结果，并在 `README.md`、`docs/RESULTS.md` 中只记录实际跑通的命令和结果。

保持项目轻量：外部源码放 `references/`，主代码放 `meter_reader/`，数据、权重和产物分别放 `data/`、`weights/`、`outputs/`。不要引入审计、哈希/指纹、测试平台、模型注册中心、复杂日志、插件框架或微服务。只接入当前采用的一条主实现路径，把时间用于识别效果与准确率。
